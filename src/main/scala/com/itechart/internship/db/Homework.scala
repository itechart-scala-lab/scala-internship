package com.itechart.internship.db

object Homework {

  // 1. Add implementation of storage in Tagless Final homework using any relational
  // database (postgres, mysql...) and Doobie as persistence layer in your app

  // 2. Add possibility to archive employees as an alternative for plain removal

  // example:
  // You can add to the `employee` table new column `is_archived`, by default this column
  // will be `false`, but if you want to archive an employee - set the flag to `true`
}
